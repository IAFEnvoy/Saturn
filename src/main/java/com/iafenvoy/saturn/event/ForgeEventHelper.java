package com.iafenvoy.saturn.event;

//? forge {
/*import net.minecraftforge.common.MinecraftForge;
import net.minecraftforge.eventbus.api.Event;
import net.minecraftforge.eventbus.api.EventPriority;

import java.util.function.Consumer;

public final class ForgeEventHelper {
    public static <T extends Event> void addListener(Class<T> type, Consumer<T> consumer) {
        MinecraftForge.EVENT_BUS.addListener(EventPriority.NORMAL, false, type, consumer);
    }
}
*/